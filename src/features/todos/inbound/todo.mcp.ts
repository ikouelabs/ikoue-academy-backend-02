import type { TodoService } from "../domain/todo.service";

export function createTodoMCP(_: TodoService) {
	// Implémenter des tools pour la gestion des todos
	// Tester avec ChatGPT
	// Tester avec Claude
	// Utiliser @modelcontextprotocol/sdk
}
